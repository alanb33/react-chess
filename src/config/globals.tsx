// Global variables that should not change over the course of the program.
const Globals = {
    BOARDSIZE: 8,
    TILESIZE: 128,
    BORDER_FRACTION: 4,
    Z_INDEX: {
        BOARD: 0,
        HIGHLIGHT: 1,
        PIECE: 2,
    },
};

export default Globals;